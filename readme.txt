Step1: Need to get inside the project folder location via command prompt or windows power shell.

Step2: Then to run as a desktop application execute the cammand "npm run electron:serve".

(or)

Step3: Then to run as a web application execute the cammand "npm start".

[Note: Make sure 'npm' is installed in the computer before executing commands.]
[If it is not installed use "npm install" or "npm install --save-dev" cammand to install it]

Short objestive: 
      This project can demonstrate the user interactive dynamic search and filtering.

